import { Box, Link, Typography } from "@mui/material";

const NavBar = () => {
  return (
    <Box>
      <nav>
        <Link>
          <Typography variant="h2"> AccessiWeb</Typography>
        </Link>
      </nav>
    </Box>
  );
};

export default NavBar;
